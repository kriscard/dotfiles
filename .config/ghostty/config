# =============================================================================
# GHOSTTY CONFIGURATION - Modern, Fast Terminal
# =============================================================================

# Theme Configuration
theme = dark:Catppuccin Macchiato,light:Catppuccin Latte

# =============================================================================
# FONTS & APPEARANCE
# =============================================================================

# Font Configuration (using Nerd Font from Brewfile)
font-family = MonoLisa
font-size = 14

# Font features
font-feature = +liga  # Enable ligatures
font-feature = +calt  # Enable contextual alternates

# Line height adjustment (MonoLisa benefits from slight increase)
adjust-cell-height = 2

# =============================================================================
# SHELL INTEGRATION
# =============================================================================

shell-integration = zsh
shell-integration-features = cursor,sudo,title

# =============================================================================
# CURSOR CONFIGURATION
# =============================================================================

cursor-style = block
cursor-style-blink = true
# cursor-blink-interval = 500  # Removed: unknown field

# =============================================================================
# CLIPBOARD & SELECTION
# =============================================================================

clipboard-read = allow
clipboard-write = allow
copy-on-select = clipboard
# selection-foreground = none  # Removed: invalid value

# =============================================================================
# MACOS SPECIFIC SETTINGS
# =============================================================================

macos-option-as-alt = true
macos-titlebar-proxy-icon = hidden
macos-titlebar-style = hidden
macos-non-native-fullscreen = false

# =============================================================================
# WINDOW CONFIGURATION
# =============================================================================

# Window padding and sizing
window-padding-x = 8
window-padding-y = 6
window-width = 120
window-height = 30

# Window behavior
window-inherit-working-directory = true
window-new-tab-position = end
working-directory = home

# Split window appearance
unfocused-split-opacity = 0.85
resize-overlay = never

# =============================================================================
# MOUSE CONFIGURATION
# =============================================================================

mouse-scroll-multiplier = 3
mouse-hide-while-typing = true

# =============================================================================
# PERFORMANCE & BEHAVIOR
# =============================================================================

# Scrollback (50k lines for development work)
scrollback-limit = 50000

# Performance (gtk-single-instance removed - Linux-only setting)

# =============================================================================
# KEYBOARD SHORTCUTS
# =============================================================================

# Configuration management
keybind = super+r=reload_config

# Quick terminal (quake-style dropdown - requires accessibility permission)
keybind = global:super+backquote=toggle_quick_terminal

# Window management
keybind = super+t=new_tab
keybind = super+w=close_surface
keybind = super+n=new_window

# Sesh keybind map ctrl+a to cmk+k
keybind=super+k=text:\x01\x65

# Pane/Split management (using cmd+s as prefix like tmux)
# keybind = cmd+s>equal=new_split:right  # May cause InvalidAction
# keybind = cmd+s>minus=new_split:down   # May cause InvalidAction
# keybind = cmd+s>x=close_surface
# keybind = cmd+s>z=toggle_split_zoom
# keybind = cmd+s>e=equalize_splits

# Navigation between splits
# keybind = cmd+s>h=goto_split:left
# keybind = cmd+s>j=goto_split:bottom
# keybind = cmd+s>k=goto_split:top
# keybind = cmd+s>l=goto_split:right

# Resize splits
# keybind = cmd+s>shift+h=resize_split:left,10
# keybind = cmd+s>shift+j=resize_split:down,10
# keybind = cmd+s>shift+k=resize_split:up,10
# keybind = cmd+s>shift+l=resize_split:right,10

# Standard clipboard operations
keybind = cmd+c=copy_to_clipboard
keybind = cmd+v=paste_from_clipboard

# Navigation shortcuts
# keybind = cmd+left=text:\x01   # Beginning of line - InvalidAction
# keybind = cmd+right=text:\x05  # End of line - InvalidAction

# Fullscreen
keybind = cmd+enter=toggle_fullscreen

# Font size adjustment
keybind = cmd+plus=increase_font_size:1
keybind = cmd+minus=decrease_font_size:1
keybind = cmd+0=reset_font_size

# =============================================================================
# ADVANCED FEATURES
# =============================================================================

# Link detection and opening
auto-update = off
confirm-close-surface = false

# Text selection
# word-characters = :@-./_~?&=%+#  # Removed: unknown field

# =============================================================================
# ENVIRONMENT VARIABLES
# =============================================================================

# Theme environment for consistency with other tools
env = THEME_FLAVOUR=macchiato
# keybind = cmd+shift+enter=text:\x1b\r  # Disabled for Claude Code
keybind = shift+enter=text:\\\n\r
