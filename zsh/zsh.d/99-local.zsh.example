# Local ZSH Configuration
# Copy this file to 99-local.zsh and customize for your machine
# This file is gitignored to keep machine-specific settings private

# =============================================================================
# MACHINE-SPECIFIC ENVIRONMENT VARIABLES
# =============================================================================

# Claude Code environment (affects settings profile loaded by claude-env)
# Options: "work" (token-optimized) or "home" (full features)
# See: ~/.dotfiles/.claude/work-settings.md for details
# export CLAUDE_ENV="work"  # Uncomment on work machines

# Work-specific paths (uncomment and customize as needed)
# export WORK_DIR="$HOME/work"
# export PROJECT_ROOT="$HOME/projects"

# Custom tool paths
# export ANDROID_HOME="$HOME/Library/Android/sdk"
# export JAVA_HOME="/Library/Java/JavaVirtualMachines/zulu-17.jdk/Contents/Home"

# =============================================================================
# LOCAL ALIASES
# =============================================================================

# Project shortcuts (customize for your projects)
# alias myproject="cd ~/projects/myproject && nvim"
# alias work="cd ~/work"

# Machine-specific tool aliases
# alias python="python3"

# =============================================================================
# LOCAL FUNCTIONS
# =============================================================================

# Example: Quick project setup
# mysetup() {
#   cd ~/projects/myproject
#   git pull
#   pnpm install
#   nvim
# }

# =============================================================================
# LOCAL INTEGRATIONS
# =============================================================================

# Source additional local scripts
# [[ -f "$HOME/.local/scripts/work.zsh" ]] && source "$HOME/.local/scripts/work.zsh"

# Company VPN helpers (example)
# vpn-connect() { ... }
# vpn-disconnect() { ... }
